describe("first_example_project", () => {
  beforeEach(() => {
    // Wykonaj cy.visit tylko raz przed każdym blokiem it()
    cy.visit("https://example.com");
  });

  it("log IN", () => {});

  it(" log IN AND choose course and choose Quality Assurance", () => {});

  it("log IN AND choose course AND Click conspect, materials, classes and last but not least homework", () => {});
});
